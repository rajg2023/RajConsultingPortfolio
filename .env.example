# Client-side variables used by the app (values embedded in the built bundle)

# Required for real OAuth: URL of your serverless proxy base (no trailing slash)
# Example: https://your-proxy.example.com or http://localhost:8787
VITE_OAUTH_PROXY_URL=

# Optional (if using client-initiated authorize): public GitHub OAuth client id
# You may provide this via GitHub Pages/Actions build env as a repository/environment secret
# Note: this value becomes public in the built bundle (acceptable for client_id)
VITE_GITHUB_CLIENT_ID=

# Optional: Enable simulated admin login without OAuth/proxy
# Use only for local testing or non-sensitive deployments
VITE_DEMO_AUTH=false

# Optional: Comma-separated GitHub usernames allowed to administer
VITE_AUTHORIZED_USERS=rajg2023
